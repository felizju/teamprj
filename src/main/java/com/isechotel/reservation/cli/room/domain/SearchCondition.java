package com.isechotel.reservation.cli.room.domain;

public enum SearchCondition {

    // roomNumber, roomName, ALL, RESERVATION_FLAG, RESERVATION_NUMBER
    ROOM_NUMBER, ROOM_NAME, ALL, RESERVATION_FLAG, RESERVATION_NUMBER, USER_NAME, USER_PHONE, REGION

}
